start expressjs server with 'npm start'

copy react application build folder into client directory
    (set proxy in clients package.json correctly before building)

copy python script into network directory